---
  - hosts: loadbalancer
    become: true
    tasks:
      - name: Install Nginx
        apt: name=nginx state=present update_cache=yes

      - name: ensure nginx started
        service: name=ngiinx state=started enabled=yes